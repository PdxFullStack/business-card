// place files you want to import through the `$lib` alias in this folder.
import BusinessCard from '$lib/components/CardContents';
import VerticallyMaskedFrame from '$lib/components/VerticallyMaskedFrame';
import Root from '$lib/components/Root';

export {
  BusinessCard,
  Root,
  VerticallyMaskedFrame,
}